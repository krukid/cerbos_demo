export enum UserRole {
    USER = 'USER',
    ADMIN = 'ADMIN',
};

export enum ResourceKind {
    CONTRACT = 'contract',
};

export enum ContractAction {
    VIEW = 'contract:view',
    EDIT = 'contract:edit',
};
